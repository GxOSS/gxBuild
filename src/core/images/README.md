# Core / Images

Image manipulation before passing to builder

blocks -> Physical / Logical NAND conversion
stfs -> STFS PIRS extraction
discovery -> Hacky methods for NAND extraction - Due to be merged into the builder as legitimate methods

gxp -> gxBuild Patching Engine (GXS)
signature -> Signature patching (JSON)
xepatch -> xeBuild Patching Engine (BIN)
